# New Infinite Canvas Documentation

This documentation provides an overview of the codebase in `src/`, how the pieces fit together, and how to work with the library as a consumer and contributor.

- For a big-picture walkthrough, see `architecture.md`.
- For the public API surface, see `api.md`.
- For rendering details (WebGL, z-order, culling, low-res), see `rendering.md`.
- For input, selection, and bounding boxes, see `interaction.md`.
- For persistence and storage, see `storage.md` and `serialization.md`.
- For local development and build, see `development.md`.
